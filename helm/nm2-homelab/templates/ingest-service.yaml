apiVersion: v1
kind: Service
metadata:
  name: nm2-ingest
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "nm2.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: nm2-ingest
  ports:
    - port: {{ .Values.ingestApi.port }}
      targetPort: {{ .Values.ingestApi.port }}
  type: ClusterIP
