apiVersion: v1
kind: Service
metadata:
  name: nm2-postgres
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "nm2.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: nm2-postgres
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
  type: ClusterIP
